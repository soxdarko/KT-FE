import RegClient from '../../Components/Auth/RegClient/RegClient';

const clientRegistrationPage = props => {
	return (
		<div>
			<RegClient />
		</div>
	);
};

export default clientRegistrationPage;
